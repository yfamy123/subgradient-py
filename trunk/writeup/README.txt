Compile command example:

 latex stoch_subgrad_notes.tex
 dvipdf stoch_subgrad_notes.dvi

To include the bibliography, run

 bibtex stoch_subgrad_notes

Then latex a few times so the references get caught up.
Then dvips (to create ps) and ps2pdf14 (to create pdf).
